@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'BOM Upload: Projection Header'
/* REMOVED: @Metadata.ignorePropagatedAnnotations: true*/
@Metadata.allowExtensions: true
define root view entity ZBMU_P_HEADER 
provider contract transactional_query 
as projection on ZBMU_I_HEADER
{
    key EndUser,
    key FileId,
    FileStatus,
 @Semantics.largeObject:
      { mimeType: 'Mimetype',
      fileName: 'Filename',
      acceptableMimeTypes: [ 'application/vnd.ms-excel','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' ],
      contentDispositionPreference: #INLINE }
    Attachment,
    @Semantics.mimeType: true
    Mimetype,
    Filename,
    LocalCreatedBy,
    LocalCreatedAt,
    LocalLastChangedBy,
    LocalLastChangedAt,
    LastChangedAt,
    /* Associations */
    _XLData: redirected to composition child ZBMU_P_STAGING
   
}
